#pragma once

// Typedef: ATOM
// Unsigned Short Integer (compiler-specific size)
typedef WORD ATOM;

// Typedef: BOOL
// Signed Integer (compiler-specific size)
typedef int BOOL;

// Typedef: BYTE
// Unsigned Character (ASCII)
typedef uchar BYTE;

// Typedef: CHAR
// Character
typedef char CHAR;

// Typedef: COLORREF
// Unsigned Long Integer (compiler-specific size)
typedef DWORD COLORREF;

// Typedef: DWORD
// Unsigned Long Integer (compiler-specific size)
typedef ulong DWORD;

// Typedef: DWORD_PTR
// Unsigned Long Integer (compiler-specific size)
typedef ULONG_PTR DWORD_PTR;

// Typedef: HANDLE
// pointer to void
typedef void* HANDLE;

// Typedef: HMODULE
// pointer to HINSTANCE__
typedef HINSTANCE HMODULE;

// Typedef: HRESULT
// Signed Long Integer (compiler-specific size)
typedef long HRESULT;

// Typedef: HWND
// pointer to HWND__
typedef HWND__* HWND;

// Typedef: LONG
// Signed Long Integer (compiler-specific size)
typedef long LONG;

// Typedef: LONGLONG
// Compiler-defined 'double' IEEE 754 floating-point type (64-bit / 8-byte format, aligned-length is 8-bytes)
typedef double LONGLONG;

// Typedef: LONG_PTR
// Signed Long Integer (compiler-specific size)
typedef long LONG_PTR;

// Typedef: LPARAM
// Signed Long Integer (compiler-specific size)
typedef LONG_PTR LPARAM;

// Typedef: LPBOOL
// pointer to BOOL
typedef BOOL* LPBOOL;

// Typedef: LPBYTE
// pointer to BYTE
typedef BYTE* LPBYTE;

// Typedef: LPCH
// pointer to CHAR
typedef CHAR* LPCH;

// Typedef: LPCVOID
// pointer to void
typedef void* LPCVOID;

// Typedef: LPCWSTR
// pointer to WCHAR
typedef WCHAR* LPCWSTR;

// Typedef: LPDWORD
// pointer to DWORD
typedef DWORD* LPDWORD;

// Typedef: LPHANDLE
// pointer to HANDLE
typedef HANDLE* LPHANDLE;

// Typedef: LPSTR
// pointer to CHAR
typedef CHAR* LPSTR;

// Typedef: LPVOID
// pointer to void
typedef void* LPVOID;

// Typedef: LPWORD
// pointer to WORD
typedef WORD* LPWORD;

// Typedef: LPWSTR
// pointer to WCHAR
typedef WCHAR* LPWSTR;

// Typedef: PHANDLE
// pointer to HANDLE
typedef HANDLE* PHANDLE;

// Typedef: SHORT
// Signed Short Integer (compiler-specific size)
typedef short SHORT;

// Typedef: SIZE_T
// Unsigned Long Integer (compiler-specific size)
typedef ULONG_PTR SIZE_T;

// Typedef: UINT
// Unsigned Integer (compiler-specific size)
typedef uint UINT;

// Typedef: UINT_PTR
// Unsigned Integer (compiler-specific size)
typedef uint UINT_PTR;

// Typedef: ULONG_PTR
// Unsigned Long Integer (compiler-specific size)
typedef ulong ULONG_PTR;

// Typedef: WCHAR
// Wide-Character (compiler-specific size)
typedef wchar_t WCHAR;

// Typedef: WORD
// Unsigned Short Integer (compiler-specific size)
typedef ushort WORD;

// Typedef: WPARAM
// Unsigned Integer (compiler-specific size)
typedef UINT_PTR WPARAM;

