#pragma once

// Enum: EWeaponState
enum {
    WEAPON_STATE_LYING_AROUND = 0x0,
    WEAPON_STATE_IN_INVENTORY = 0x1,
    WEAPON_STATE_IN_HAND = 0x2,
    WEAPON_STATE_ON_HOOK = 0x3
};
typedef int EWeaponState;

