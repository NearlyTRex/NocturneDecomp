#pragma once

// Enum: EWeaponState
enum {
    WEAPON_STATE_LYING_AROUND = 0,
    WEAPON_STATE_IN_INVENTORY = 1,
    WEAPON_STATE_IN_HAND = 2,
    WEAPON_STATE_ON_HOOK = 3
};
typedef int EWeaponState;

