#pragma once

// Enum: ETurretState
enum {
    TURRET_STATE_IDLE = 0,
    TURRET_STATE_CHARGING = 1,
    TURRET_STATE_ACTIVE = 2,
    TURRET_STATE_PATROL = 3,
    TURRET_STATE_POWERING_DOWN = 4
};
typedef int ETurretState;

